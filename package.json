{
  "name": "gemmra-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node -e \"const fs = require('fs'); ['dist/_redirects', 'dist/client/_redirects'].forEach(f => fs.existsSync(f) && fs.unlinkSync(f))\"",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^6.3.1"
  },
  "dependencies": {
    "@xterm/xterm": "6.0.0"
  }
}

